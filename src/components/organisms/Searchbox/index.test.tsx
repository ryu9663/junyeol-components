import { describe } from "vitest";

describe("Searchbox", () => {
  it("Searchbox에 타이핑하면 options를 담은 dropdown이 나타난다.", () => {});

  it("Searchbox가 블러되면 dropdown이 사라진다.", () => {});

  it("Searchbox에 value,onChange가 없어도 정상 작동한다.", () => {});

  it("Searchbox에서 검색어를 입력하면 options에서 해당 검색어를 포함하는 요소만 dropdown에 나타난다 .", () => {});

  it("options에서 선택한 값으로 Searchbox의 value가 결정된다.", () => {});
});
